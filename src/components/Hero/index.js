import React from 'react';

function Hero() {
    return (
        <section className="hero">
            <div>
                <h3>Connect for your website needs!</h3>
            </div>
        </section>
    );
}

export default Hero;